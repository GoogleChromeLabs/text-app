TD.controller('SideBar', function($scope, tabs) {
  $scope.tabs = ['Untitled 1', 'File.txt', 'The Third File.txt'];
  
  $scope.new = function() {
  };

  $scope.open = function() {
  };

  $scope.saveas = function() {
  };
});
